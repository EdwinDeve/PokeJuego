@model IEnumerable<PokeJuego.Models.C_tbTipoPokemon>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table ">
    <tr class="bg-primary">
        <th>
            <label>
                Tipo de pokemon
            </label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr class="bg-info">
        <td>
            @Html.DisplayFor(modelItem => item.strTipoPokemon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intTipoPokemon, }) |
            @Html.ActionLink("Details", "Details", new { id=item.intTipoPokemon }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intTipoPokemon })
        </td>
    </tr>
}

</table>
